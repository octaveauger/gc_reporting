$('#results').append("<%= j render partial: 'mandates/mandate', collection: @mandates %>");
<% if @mandates.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @mandates %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>
initialize();