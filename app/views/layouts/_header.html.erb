<nav class="navbar navbar-default navbar-fixed-top" role="navigation" id="site-navbar">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#top-navbar">
          <span class="sr-only"><%= t('header.toggle') %></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
      </button>
    </div>

    <div class="collapse navbar-collapse" id="top-navbar">
      <ul class="nav navbar-nav">
          <li><%= link_to t('header.app_name'), root_path, class: 'brand' %></li>
        <% if signed_in? %>
          <li><%= link_to t('header.link_mandates'), reporting_mandates_path %></li>
          <li><%= link_to t('header.link_payments'), reporting_payments_path %></li>
          <li><%= link_to t('header.link_payouts'), reporting_payouts_path %></li>
        <% end %>
      </ul>
      <ul class="nav navbar-nav pull-right">
        <% if signed_in? %>
  	        <li><%= link_to t('header.link_logout'), logout_connect_path %></li>
  	    <% else %>
  	        <li class="list-button"><%= link_to t('header.link_connect'), authorise_connect_path, :class => 'btn pop' %></li>
  	    <% end %>
      </ul>
      <ul class="nav navbar-nav pull-right">
        <%= link_to image_tag('blank.png', :class => 'flag flag-gb'), locale: 'en' %>
        <%= link_to image_tag('blank.png', :class => 'flag flag-fr'), locale: 'fr' %>
      </ul>
    </div>
  </div>
</nav>