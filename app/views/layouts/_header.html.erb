<nav class="navbar navbar-default navbar-fixed-top" role="navigation" id="site-navbar">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#top-navbar">
          <span class="sr-only"><%= t('header.toggle') %></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
      </button>
    </div>

    <div class="collapse navbar-collapse" id="top-navbar">
      <ul class="nav navbar-nav">
          <li><%= link_to image_tag('logo.png', height: '20', width: '90'), root_path, class: 'brand' %></li>
        <% if signed_in? %>
          <li><%= link_to t('header.link_mandates'), reporting_mandates_path %></li>
          <li><%= link_to t('header.link_payments'), reporting_payments_path %></li>
          <li><%= link_to t('header.link_payouts'), reporting_payouts_path %></li>
        <% end %>
      </ul>
      <ul class="nav navbar-nav pull-right">
        <% if signed_in? %>
  	        <li><%= link_to t('header.link_logout'), logout_connect_path %></li>
  	    <% else %>
            <li class="list-button"><%= link_to t('header.link_login'), authorise_connect_path %></li>
            <li class="list-button"><%= link_to t('header.link_signup'), signup_path, :class => 'btn pop' %></li>
  	    <% end %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
            <%= image_tag('blank.png', :class => 'flag ' + locale_flag(I18n.locale.to_s)) %>
            <span class="caret"></span>
          </a>
          <ul class="dropdown-menu dropdown-flags">
            <li><%= link_to image_tag('blank.png', :class => 'flag flag-gb'), locale: 'en' %></li>
            <li><%= link_to image_tag('blank.png', :class => 'flag flag-fr'), locale: 'fr' %></li>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>