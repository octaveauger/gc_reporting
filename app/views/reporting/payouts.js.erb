$('#results').append("<%= j render partial: 'reporting/payout', collection: @payouts %>");
<% if @payouts.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @payouts %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>
initialize();