$('#results').append("<%= j render partial: 'reporting/payment', collection: @payments %>");
<% if @payments.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @payments %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>
initialize();